const express = require('express');
const router = express.Router();
const ShipmentRepository = require('../repositories/shipmentRepository');

// CRUD routes here following the above pattern
module.exports = router;
